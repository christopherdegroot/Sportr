// test doc for chakra

import { VStack, Heading } from "@chakra-ui/react";

const Cart = () => {
  return (
    <VStack w="full" h="full" p={10} spacing={10} alignItems="center">
      <Heading>Event Feed</Heading>
    </VStack>
  );
};

export default Cart;
